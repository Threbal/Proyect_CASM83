{
  "compilerOptions": {
    // Paths
    "rootDir": "./src",           // Punto de entrada (src/)
    "outDir": "./dist",           // Genera el código compilado en dist/

    // Node ESM real
    "module": "ES2022",           // Para NodeNext y ESModules
    "moduleResolution": "node",   // Resuelve módulos de manera adecuada
    "target": "ES2022",           // Mejor compatibilidad con Node 18+
    "lib": ["ES2022"],            // Librerías estándar de ES2022

    // Interop / calidad
    "esModuleInterop": true,      // Importa módulos CJS sin problemas
    "strict": true,               // Habilita todas las verificaciones estrictas
    "skipLibCheck": true,         // Omite la verificación de librerías

    // Mapas opcionales (mejor rendimiento si no los usas)
    "sourceMap": false,           // Desactiva si no necesitas mapear
    "declaration": false,         // No genera archivos .d.ts
    "declarationMap": false       // Desactiva mapas de declaración
  },
  "include": ["src"]              // Solo incluye la carpeta src/
}
